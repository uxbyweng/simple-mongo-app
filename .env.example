# Für lokale Entwicklung ohne Docker (npm run dev):
MONGODB_URI=mongodb://localhost:27017/simple-mongo-app

# Für docker compose wird MONGODB_URI stattdessen direkt
# in der docker-compose.yml auf den "mongo"-Service gesetzt.
